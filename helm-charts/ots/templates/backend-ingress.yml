apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{ .Values.otsNamespace }}-backend"
  namespace: "{{ .Values.otsNamespace }}"
  labels:
    {{- range $k, $v := .Values.labels }}
    {{ $k }}: {{ $v | quote }}
    {{- end }}
spec:
  rules:
    - host: "{{ .Values.otsBackendIngressAddress }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "{{ .Values.otsNamespace }}-backend"
                port:
                  number: 5000